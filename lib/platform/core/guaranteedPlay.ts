/** Guaranteed Play — boards always complete at 85% via platform-owned entries. */

export const GUARANTEED_FILL_PERCENT = 85;
export const GUARANTEED_FILL_SQUARES = 85;
export const TOTAL_BOARD_SQUARES = 100;

export const PLATFORM_SQUARE_OWNER_LABEL = "SquareBoards";

export const GUARANTEED_PLAY_EXPLAINER =
  "When a board reaches 85% sold, SquareBoards automatically reserves the remaining squares with platform-owned entries so the game always runs. Players never experience cancelled games due to slow fill rates.";
